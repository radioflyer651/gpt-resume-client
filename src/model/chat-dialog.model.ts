

export interface ChatDialog {
    /** Gets or sets the name of the actor that this chat is with. */
    chatActor: string;

    /** Gets or sets the dialog between the user and the actor. */
    dialog: string;
}